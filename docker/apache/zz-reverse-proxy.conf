# Traefik (or any TLS terminator) speaks HTTP to this container. Expose HTTPS to PHP
# via mod_setenvif so $_SERVER['HTTPS'] is set before wp-config runs.
<IfModule mod_setenvif.c>
	SetEnvIfNoCase X-Forwarded-Proto https HTTPS=on
</IfModule>
